#!/bin/bash
ls -ltrah --color=auto --group-directories-first

# En esta versión más detallada;
# color=auto habilita el resaltado de colores según el tipo de archivo.
# group-directories-first agrupa los directorios primero en la lista.
